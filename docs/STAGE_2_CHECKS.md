# Этап 2 — интерфейс и серверная основа

Дата проверки: 17 сентября 2026 года. Рабочая база не заполнялась тестовыми аккаунтами: браузерные проверки выполнялись на отдельном сервере с SQLite в памяти.

## Выполнено

- Светлая и тёмная темы; переключатель в шапке вместо смены роли.
- Увеличенные подписи и масштаб текста 100%, 115%, 130%; настройки сохраняются на устройстве.
- Единые цвета шести характеристик, тегов предметов и примеров навыков. Текстовые названия сохраняются.
- Fastify и SQLite; локальный первый аккаунт мастера, вход/выход, одноразовые приглашения и членство в кампаниях.
- Серверная проверка прав, фильтрация секретных данных и личных заметок, версии записей и идемпотентность операций.
- Подключение существующих действий интерфейса к API, история с отменой, уведомления WebSocket и резервное обновление снимка каждые 5 секунд.

## Автоматические проверки

`pnpm test` — 12 успешных тестов: первичная настройка и источник запросов; вход и отзыв сессии; ограничения игрока; отсутствие секретных данных в API; изоляция кампаний и отзыв членства; одноразовые приглашения; конфликты версий и повтор операций; отмена; сохранность SQLite после перезапуска; авторизация WebSocket; ограничение попыток входа; конфликты заметок.

`pnpm build` — TypeScript и production-сборка проходят.

## Браузерная проверка

- Вход в отдельные тестовые аккаунты мастера и игрока.
- Мастер: изменение здоровья 12 → 9, запись истории и отмена.
- Игрок: отсутствуют кнопки изъятия предметов и скрытое свойство компаса; личная заметка сохраняется на сервере и читается после перезагрузки.
- Светлая и тёмная темы проверены визуально на листе и инвентаре.
- Настройки с масштабом 130% проверены при viewport 390 × 844: горизонтального переполнения страницы нет, основной шрифт 20.8 px.
- Исправлено сжатие блока названия кампании в боковом меню.

## Границы результата

Это завершение серверной основы и согласованных правок интерфейса, не готовый релиз всей игры. Tailscale/HTTPS на устройствах группы, автоматические копии, редактор контента, портреты и полный движок SRD остаются последующими этапами. Анкета и заметки пока сохраняются кнопкой. Цветные теги обозначают связь с характеристикой, но не применяют бонусы.


### Дополнение: редактор предметов
- Сборка TypeScript/Vite проходит; 13 серверных тестов проходят.
- Новый тест проверяет запрет редактирования игроком, изоляцию кампаний, скрытие библиотеки от игрока, валидацию тегов, конфликт версий и независимость выданной копии от шаблона.
- В отдельной браузерной кампании проверены создание предмета, произвольный тип и фильтр, тег Интеллекта, скрытое свойство и повторное открытие редактора с сохранёнными полями.
- Схема SQLite v2 добавляет library_items без удаления прежних данных.

### 18 сентября: заклинания и особенности
- TypeScript/Vite: сборка успешна. Сервер: 14 тестов, все проходят.
- Проверены запрет записи игроком, изоляция библиотек кампаний, конфликт версий и валидация вида/круга/тегов карточки.
- Проверены выдача с повторным идентификатором операции без дубля, независимость экземпляров от изменения шаблона, удаление и отмена, чтение игроком.
- Тест дисковой SQLite дополнен сохранением шаблона и выданной способности после перезапуска.
- В изолированном браузерном стенде созданы заклинание и особенность, проверены поиск, выдача и их отдельное отображение в листе. Визуально проверена тёмная тема.
- В браузере под игроком подтверждены обе выданные карточки; кнопок удаления и доступа к библиотеке нет. Тестовый стенд остановлен, его временные данные удалились вместе с процессом.

### Состояние инвентаря
- Сборка TypeScript/Vite успешна; 15 серверных тестов прошли.
- Новый тест: права игрока, недопустимые количества/заряды/настройка, чтение состояния без утечки секрета, конфликт версий, отмена, разделение стопки и повтор запроса без дублирования.
- В изолированной браузерной кампании проверены создание шаблона с 5 зарядами и настройкой, выдача полного запаса, форма состояния, экипировка, настройка и сохранение 2 оставшихся зарядов. Старые предметы отображаются без ошибок.
- Браузер: стопка из 5 зелий разделена на 4 + 1, число позиций увеличилось с 4 до 5, общее количество осталось 8. Тестовый сервер остановлен.

### Базовые расчёты
- Сборка TypeScript/Vite успешна, 19 тестов прошли.
- Тесты правил: уровни 1–20, отрицательное округление, крайние характеристики, владение/экспертиза, спасброски, инициатива и пассивная внимательность.
- Интеграционный тест: права, валидация параметров, запрет записи производных итогов, конфликт версий, сохранение текущих хитов и пересчёт после отмены.
- В браузере проверены редактор мастера, уровень 5 и бонус +3, Мудрость 18 с экспертизой во внимательности (пассивное значение 20); максимум хитов увеличен до 30 при сохранении текущих 12. Тестовый стенд остановлен.

### Первый шаблон создания по SRD
- Сборка TypeScript/Vite успешна; 20 тестов проходят.
- Новый тест проверяет допустимость сочетания, бонусов, навыков, языков и мастерства оружия; отклоняет повторяющиеся выборы и завышенный массив.
- Проверены серверное назначение хитов/КД/владений, игнорирование подменённых итогов, отсутствие дублей при сохранении, запрет смены способа создания сохранённого персонажа, отправка и утверждение.
- В отдельном браузерном стенде игрок создал Борина Каменного, сохранил и заново открыл анкету: исходный массив остался 15/14/13/12/10/8, итоговый — 17/14/14/12/10/8, хиты 13, КД 12. После отправки статус «На утверждении», редактирование закрыто.
- Начальный расчёт КД явно отделён от последующих ручных правок. Тестовый стенд остановлен, рабочие кампании не изменялись.

### Перенос и архив библиотек
- Сборка TypeScript/Vite успешна; 21 серверный/расчётный тест прошёл.
- Новый тест проверяет права мастера, скрытые свойства в разрешённом экспорте, отсутствие персонажей в файле, архив и восстановление, неизменность инвентаря, архив начальных карточек, независимость кампаний и версии.
- Импорт: новые ID, отклонение некорректного набора без частичной записи, повтор той же операции без дублей.
- Браузер: начальный меч убран в архив и восстановлен. Экспорт активных трёх предметов вставлен в импорт, показан предварительный список, созданы три новые копии. После восстановления меч снова виден, лечебных зелий в библиотеке две карточки. Проверка выполнена на изолированном стенде; рабочие данные не менялись.
- JSON-файл через системный диалог и фактическое скачивание отдельно не автоматизировались; проверены подготовка экспортного текста и импорт через поле JSON.
# Проверка блока резервирования — 18 сентября 2026

- Общий набор: 23 теста прошли; `pnpm build` завершился успешно.
- На временной базе проверены согласованный снимок при открытом WAL, сохранение персонажа и секретной/личной заметок, восстановление прежнего состояния, защитная копия до замены, сброс сессий и приглашений, повторный запуск.
- Проверены запрет восстановления при активном сервере, отклонение повреждённого файла и несовместимой версии без изменения рабочей базы, хранение 10 автоматических снимков и сохранение ручных.
- Рабочий сервер отвечает HTTP 200; первая автоматическая копия создана. Рабочая база не восстанавливалась. Интерфейс не менялся; новые команды проверены через Node и локальный CLI.

## Защита ввода и связь — 18 сентября 2026

Сборка TypeScript/Vite прошла. В браузере на отдельной кампании проверены предупреждение при уходе из заметок, отмена перехода и смены вида заметки, успешное сохранение с последующей навигацией без предупреждения. В анкете проверено сохранение введённого имени после «Остаться» и переход после «Уйти без сохранения». Рабочие данные не изменялись. Добавлены повторное подключение WebSocket, обработчики online/offline/focus и ручная проверка связи. Длительный сетевой разрыв и нативный диалог закрытия вкладки в этой проверке не воспроизводились.

## Диалоги и фильтры истории — 18 сентября 2026

Сборка TypeScript/Vite прошла. На отдельной тестовой кампании в браузере проверены изменённые формы предмета и способности: закрытие вызывает предупреждение, «Остаться» сохраняет введённое название, подтверждённый уход закрывает форму. Проверены поиск без результатов, сброс, выбор персонажа, отмена события и фильтр отменённых записей. Интерфейс истории осмотрен на скриншоте. Защита остальных диалогов использует общий обработчик; все отдельные формы и Escape в этом проходе не проверялись. Серверный код не менялся, полный серверный набор повторно не запускался.

## Ресурсы — 18 сентября 2026

24 серверных теста прошли; сборка TypeScript/Vite успешна. Новый сценарий проверяет права мастера/игрока, неверный круг, дубли ID, остаток выше максимума, расход пустого ресурса, конфликт версий, частичное восстановление коротким отдыхом, полное долгим, повтор операции без дубля, отсутствие изменения здоровья, отмену и видимость игроку. В браузере на отдельной кампании проверены создание пула ячеек, сохранность формы после отказа от закрытия, сохранение, расход 2→1, предварительный просмотр долгого отдыха и восстановление 1→2. Рабочие игровые данные не изменялись.

## Числовые поправки — 18 сентября 2026

26 тестов прошли: сложение положительных/отрицательных поправок, игнорирование выключенных, изоляция целей, пересчёт пассивной внимательности и неизменность базы; серверные права, валидация, дубли, конфликт, видимость игроку и отмена отключения. Сборка TypeScript/Vite успешна. На тестовой кампании в браузере настроена поправка +2 к КД: получено 16 при базе 14 с источником «Защитное кольцо»; проверена защита ввода при закрытии редактора. Рабочие игровые данные не изменялись.

## Эффекты экипировки — 18 сентября 2026

28 тестов прошли; сборка TypeScript/Vite успешна. Проверены экипировка/настройка, сложение с ручными поправками, пересчёт навыка и пассивной внимательности, права игрока, валидация цели/значения/дублей, отсутствие скрытого текста в расчётах, отмена снятия предмета и независимость выданной копии от изменения шаблона. В браузере на отдельной тестовой кампании создан и выдан предмет с +2 к КД: в сумке эффекты не действуют, после экипировки КД вырос с 14 до 16 с названием предмета-источника. Рабочие игровые данные не менялись.

## Состояния и концентрация — 18 сентября 2026

29 тестов прошли; сборка TypeScript/Vite успешна. Проверены права мастера/игрока, обязательное название, дубли ID, длина длительности, строгая схема концентрации, конфликт версий, снятие с сохранением остальных игровых параметров, отмена и видимость игроку. Проверка перезапуска SQLite дополнена сохранением концентрации. В браузере на отдельной кампании проверены добавление состояния и концентрации, защита несохранённого ввода, сохранение, отображение состояния в листе и кратких отметок в группе. Рабочие данные не изменялись.

## Вес инвентаря — 18 сентября 2026

31 тест прошёл; сборка TypeScript/Vite успешна. Проверены сумма с количеством, целые граммы, нулевой/неизвестный вес, разделение стопки без изменения суммы, права игрока, серверные ограничения, хранение шаблона и отмена очистки веса. В браузере на отдельной кампании проверено 1,25 кг × 3 = 3,75 кг с отметкой одного неизвестного предмета; ввод нуля проверен. Первоначальная автоматическая проверка очистки поля не подтвердила результат; окончательная проверка описана ниже. Рабочие данные не изменялись.

## Завершение проверки веса

При возобновлении работы обнаружено, что автоматический вызов заполнения числового поля пустой строкой не обновлял состояние формы. Очистка настоящими клавишами Ctrl+A / Backspace проверена в браузере: после сохранения отображается «Вес не указан». Добавлена отдельная кнопка «Очистить вес» и обработка ввода через onInput; кнопка также проверена с последующим сохранением. Проверки проводились на отдельной временной базе.
